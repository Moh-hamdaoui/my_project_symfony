<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Liste des Recettes{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        .header {
            background-color: #e3f2fe;
            padding: 15px 0;
            text-align: center;
        }
        .header h3 {
            margin: 0;
            font-size: 1.5rem;
            font-weight: 600;
        }
    </style>
</head>
<body>
    <header class="header shadow-sm">
        <h3>Bienvenue sur le site de recettes !</h3>

        <div class="mt-3">
            {% if app.user %}
                <p>Bienvenue, {{ app.user.email }}!</p> 
                <a href="{{ path('app_logout') }}" class="btn btn-danger">Déconnexion</a>
            {% else %}
                <!-- Affiche les boutons Connexion et Inscription si l'utilisateur n'est pas connecté -->
                <a href="{{ path('app_login') }}" class="btn btn-primary">Se connecter</a>
                <a href="{{ path('app_register') }}" class="btn btn-success">S'inscrire</a>
            {% endif %}
        </div>
    </header>

    <!-- Contenu spécifique à chaque page -->
    <div class="container my-4">
        {% block content %}{% endblock %}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
